generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Study {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  nickname String
  protocol String
  queries  Int
  sponsor  String
  status   String
  subjects Int
}

model User {
  id      String   @id @default(auto()) @map("_id") @db.ObjectId
  name    String
  studies String[]
}
